<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<DATconfig>
    <ID>1000</ID>
    <name>Data Analysis Tool</name>
	<data>   
        <sql>
            select SUM(Hours) AS Individual_Total_Hours, PersonnelNo, Name, CostCenter, LM, (SUM(Hours) / 168) AS UR from CDT GROUP BY PersonnelNo, Name, CostCenter, LM order by UR DESC limit 50;
        </sql> 
		<rowFormat>s|d|d|d</rowFormat>
		<sheetName>Remote percentage to per region</sheetName>
		<colCount>4</colCount>
    </data>
	<template>302_Excel_Tool.xls</template>
    <output>302_Excel_Tool_${endTime:yyMMdd}.xls</output>
</DATconfig>