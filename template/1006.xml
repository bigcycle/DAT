<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<DATconfig>
    <ID>1006</ID>
    <name>Data Analysis Tool</name>
	<data>   
        <sql>
            select SUM(Hours) AS Individual_Total_Hours, PersonnelNo, Name, CostCenter, LM, (SUM(Hours) / target_hours) AS UR from CDT where PersonnelNo != '#' GROUP BY PersonnelNo, Name, CostCenter, LM order by UR DESC limit 50;
        </sql> 
		<rowFormat>s|d|d|d</rowFormat>
		<sheetName>Top 50 individual UR in ITC</sheetName>
		<colCount>4</colCount>
    </data>
	<template>302_Excel_Tool.xls</template>
    <output>302_Excel_Tool_${endTime:yyMMdd}.xls</output>
</DATconfig>